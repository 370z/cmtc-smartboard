<html>

<head>
  <style>
    h1 {
      color: green;
      font-size: 70px;
    }
  </style>
</head>
<style>
  .button {
    background-color: gray;
    /* Green */
    border: none;
    color: white;
    padding: 16px 40px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 25px;
    margin: 20px 2px;
    cursor: pointer;
    outline: none;
    border-radius: 15px;
    box-shadow: 0 9px #999;
  }

  .button:hover {
    background-color: #3e8e41
  }

  .button:active {
    background-color: #3e8e41;
    box-shadow: 0 5px #666;
    transform: translateY(15px);
  }

  .wrapper {
    text-align: center;
  }

  .btnon {
    padding: 8px 22px;
  }

  .btnoff {
    padding: 8px 15px;
  }
</style>

<body>
  <header><img src="./hea.png"></header>
  <div class="background-image"></div>
  <form action="" method="POST">
    <div class="wrapper">
      <h3>อ.ขวัญดารินทร์ จิตหาญ</h3>
      <button class="button btnon" type="submit" name="ON1">ON</button><br>
      <button class="button btnoff" type="submit" name="OFF1">OFF</button>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">อ.อมรินทร์&nbsp; เลชะวณิชย์</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON2">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF2">OFF</button></a>
          </p>
        </div>

        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">อ.อนุชาติ&nbsp; รังสิยานนท์</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON3">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF3">OFF</button></a>
          </p>
        </div>

        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">อ.ธฤต&nbsp; ไชยมงคล</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON4">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF4">OFF</button></a>
          </p>
        </div>

        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">อ.ฐาปนันท์&nbsp; ปัญญามี</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON5">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF5">OFF</button></a>
          </p>
        </div>




<div class="wrapper">

  <h4>ครูเยาวเรศ ช้อนเขียว</h4>
  <button class="button btnon" type="submit" name="ON6">ON</button><br>
  <button class="button btnoff" type="submit" name="OFF6">OFF</button>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2 style="text-align: center;"><span style="font-size: 14pt;">ครูจิรัชญา&nbsp; เดชะปัญ</span></h2>
      <p style="text-align: center;"><br></p>
      <p style="text-align: center;"><button class="button btnon" type="submit" name="ON7">ON</button><br>
        <button class="button btnoff" type="submit" name="OFF7">OFF</button></a>
      </p>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">ครูอัศวินท์&nbsp; รัตนวรางค์กุล</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON8">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF8">OFF</button></a>
          </p>
        </div>

        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">ครูธีรภัทร์&nbsp; ชมภูนุช</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON9">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF9">OFF</button></a>
          </p>
        </div>

        <div class="col-md-3">
          <h2 style="text-align: center;"><span style="font-size: 14pt;">ครูจิรภัทร&nbsp; นิลโขง</span></h2>
          <p style="text-align: center;"><br></p>
          <p style="text-align: center;"><button class="button btnon" type="submit" name="ON10">ON</button><br>
            <button class="button btnoff" type="submit" name="OFF10">OFF</button></a>
          </p>
        </div>



        <div class="container-fluid">
          <footer>
            <div class="container-fluid">
              <hr>
              <div class="row"> </a>&nbsp;&nbsp;</div>
              <div class="col-md-2 hidden-sm hidden-xs text-center"> <a href="#top" title="To Top" style="text-decoration:none;"> </a><br><br> </div>
              <div class="col-md-5 div-social hidden-sm hidden-xs"> <br><br><br>
                <ul class="list-inline social-buttons"> </i></a></li>
                </ul>
              </div>
              <div class="col-md-12 visible-sm visible-xs text-center"> <a href="#top" title="To Top" style="text-decoration:none;"> <span class="h2"><i class="glyphicon glyphicon-chevron-up"></i></span> </a><br><br> สาขาวิชาเทคโนโลยีสารสนเทศ วิทยาลัยเทคนิคเชียงใหม่ <br>Information Technology Of Chiangmai Technology College<br>ติดต่อเว็บมาสเตอร์ : rungsiyanon@gmail.com<br><a href="https://play.google.com/store/apps/details?id=com.itcmtc" target="_blank"><img src="http://it.cmtc.ac.th/web2017/photo/upload/2018/1527395162_6352-org.png" width="184px" height="40px" border="0"></a><a href="#" target="_blank"><img src="http://it.cmtc.ac.th/web2017/photo/upload/2018/1527395162_4227-org.png" width="184px" height="40px" border="0"></a> <br><br>
                <ul class="list-inline social-buttons">
                  <li><a href="https://www.facebook.com/itcmtc/" target="_blank" rel="nofollow external" title="facebook"><i class="fa fa-facebook"></i></a></li>
                  <li><a href="https://www.youtube.com/channel/UCBConRkLj26HqthOpRIQrQQ" target="_blank" rel="nofollow external" title="youtube"><i class="fa fa-youtube"></i></a></li>
                </ul>
              </div>
            </div>
            </footer>
        </div>


     
      <link rel="canonical" href="http://it.cmtc.ac.th/web2017/%E0%B8%9A%E0%B8%A3%E0%B8%B4%E0%B8%81%E0%B8%B2%E0%B8%A3%E0%B8%99%E0%B8%B1%E0%B8%81%E0%B8%A8%E0%B8%B6%E0%B8%81%E0%B8%A9%E0%B8%B2/%E0%B8%AB%E0%B9%89%E0%B8%AD%E0%B8%87%E0%B9%80%E0%B8%A3%E0%B8%B5%E0%B8%A2%E0%B8%99%E0%B8%AD%E0%B8%AD%E0%B8%99%E0%B9%84%E0%B8%A5%E0%B8%99%E0%B9%8C">
      <link rel="dns-prefetch" href="//cdnjs.cloudflare.com">
      <link href="http://it.cmtc.ac.th/web2017/corecss.css" rel="stylesheet" type="text/css">
      <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" type="text/css">
      <link href="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.11.4/themes/smoothness/jquery-ui.min.css" rel="stylesheet" type="text/css">
      <link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/css/select2.min.css" rel="stylesheet" type="text/css">
      <link href="http://it.cmtc.ac.th/web2017/templates/cszdefault/css/cszdefault.min.css" rel="stylesheet" type="text/css">
      <link href="http://it.cmtc.ac.th/web2017/templates/cszdefault/css/logo.slider.css" rel="stylesheet" type="text/css">
      <script type="text/javascript" src="http://it.cmtc.ac.th/web2017/corejs.js"></script>
      <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery.lazy/1.7.5/jquery.lazy.min.js"></script>
      <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/js/select2.full.min.js"></script>
      <script type="text/javascript">
        $(function() {
          $(".lazy").lazy();
          $(".select2").select2()
        });
        $("#sel-chkbox-all").change(function() {
          $(".selall-chkbox").prop("checked", $(this).prop("checked"))
        });
      </script>


      </form>


      <?php

      $server   = "49.229.108.147:3833";  // Change this to correspond with your database port
      $username   = "root";      // Change if use webhost online
      $password   = "5Fla+EAgB5ebb8fQFtDGIQ==";
      $DB     = "qlda";      // database name


      $update = new mysqli($server, $username, $password, $DB);  // Check database connection
      if ($update->connect_error) {
        die("Connection failed: " . $update->connect_error);
      }


      if (isset($_POST['ON1']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led1`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF1']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led1`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }
      if (isset($_POST['ON2']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led2`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF2']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led2`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['ON3']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led3`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF3']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led3`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }
      if (isset($_POST['ON4']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led4`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF4']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led4`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }
      if (isset($_POST['ON5']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led5`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF5']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led5`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['ON6']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led6`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF6']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led6`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['ON7']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led7`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF7']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led7`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }


      if (isset($_POST['ON8']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led8`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF8']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led8`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }
      if (isset($_POST['ON9']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led9`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF9']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led9`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['ON10']))      // If press ON
      {

        $sql = "UPDATE `status` SET `led10`=1";  // Update present status to database
        // If don't put this If , we can't change the value in database
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 1;						// why i have to put this line but it still run when it's commented
        }
      }

      if (isset($_POST['OFF10']))    // If press OFF
      {

        $sql = "UPDATE `status` SET `led10`=0";  // Update present status to database
        // Echo "0" , equivalent with send data to App to toast OFF
        if ($update->query($sql) === TRUE) {  // Because it's been a long time , so i forgot
          //$_GET[] = 0;						// why i have to put this line but it still run when it's commented
        }
      }


      ?>


      <center><footer><img src="./foo.png" alt=""></footer></center>
      </body>


</html>